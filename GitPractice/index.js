const me = ['a', 'b', 'c'];

function Temoo(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
    console.log("Done!");
}

Temoo(me);

// All Good

// now we have another bad line of code 